services:
  postgres:
    image: postgres:16
    container_name: gymtrack-postgres
    environment:
      POSTGRES_DB: gymtrack
      POSTGRES_USER: gymtrack
      POSTGRES_PASSWORD: gymtrack
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
